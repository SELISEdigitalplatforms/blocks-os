<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
  </PropertyGroup>
	<ItemGroup>
		<!--<PackageReference Include="MongoDB.Driver" />-->
		<PackageReference Include="SeliseBlocks.Genesis.OS" />
		<PackageReference Include="FluentValidation" />
		<!-- Parses a stored message (RawMime) into its HTML body and attachment list for display. -->
		<PackageReference Include="MimeKit" />
	</ItemGroup>

	<ItemGroup>
		<!-- Captcha configuration links to a secret value; ISecretService owns the vault. -->
		<ProjectReference Include="..\Blocks.Secrets\Blocks.Secrets.csproj" />
	</ItemGroup>
</Project>
